## Создание базы данных оценок студентов по предметам

*Цели занятия:*  
1. Познакомиться с новым инструмерном проектирования и работы с БД - `phpMyAdmin`
2. Необходимо создать базу данных `Grade-IS221`, 
которая содержит две таблицы `Students` (инфа о студентах) и `Grades` (инфа о их оценках)    
двумя способами:
- с помощью визуальных средств проектирования (в phpMyAdmin)
- с помощью языки запросов SQL, конструкции вида
```
Запрос на создание
CREATE TABLE <название таблицы> (
    id INT AUTO_INCREMENT,
    name VARCHAR(50),
    ...
    PRIMARY KEY('id')
);

Запрос на вставку
INSERT INTO <название таблицы> (id, ...) VALUES
(1, 'Иван', .. ),
(2, 'Мария', ...);
```

### Задание 1. - Создание БД

Учебное видео 
1. Запустите сервер СУБД `mySQL` и веб-сервер `Apache` через `XAMPP Control panel` - нажмите кнопку start для mySql, Apache
2. Запустите `phpMyAdmin` - нажав кнопку `admin` напротив `mySQL`
3. Создайте базу данных `Grade-IS221`, затем таблицу `students` с 4 столбцами  
7. Запишите их
```
Таблица "студенты":
- id (идентификатор студента) - id INT (A_I + PRIMARY)
- имя (имя студента) - name VARCHAR(50)
- возраст (возраст студента) - age INT
- курс (курс, на котором учится студент) - course INT
```
6. Вторую таблицу `grades` создайте с помощью запроса - вкладка `SQL`
```
Шаблон запроса на создание
CREATE TABLE <название таблицы> (
    id INT AUTO_INCREMENT,
    ...
    PRIMARY KEY('id')
);

Таблица "оценки" grades:
- id (идентификатор оценки) - id INT
- student_id (идентификатор студента) - student_id INT
- предмет (название предмета) - subject VARCHAR(50)
- оценка (оценка по предмету) - grade INT
```

### Задание 2. Вставка данных

Шаблон запроса на вставку данных:  
`INSERT INTO <название таблицы> (...) VALUES (...), (...);`

1. Вставьте в таблицу `students` следующие данные:  
```
Напишите и выполните запрос:
для полей (id, name, age, course)
вставьте значения
(1, 'Иван', 16, 2),
(2, 'Мария', 15, 1),
(3, 'Петр', 15, 1);
```
2. Вставьте в таблицу `grades` следующие данные:  
```
Напишите и выполните запрос:
для полей (id, student_id, subject, grade)
вставьте значения
(1, 1, 'Математика', 4),
(2, 2, 'Математика', 3),
(3, 1, 'Информатика', 5),
(4, 3, 'Математика', 3),
(5, 1, 'Информатика', 4);
```


### Задание 3. Добавление индекса 

1. Откройте в `phpMyAdmin` вкладку `Структура` и таблицу `grades`
2. Нажмите кнопку `Вперед` в разделе `Индексы \ Создать индекс для 1 столбцов`
3. Назовите индекс `student_key` - тип `INDEX` поле `student_id`

### Задание 4. Выборка со связью 2 таблиц

Чтобы связать две таблицы используют оператор JOIN (LEFT JOIN)
```
SELECT column_name(s)
FROM table1
LEFT JOIN table2
ON table1.column_name = table2.column_name;
```
в нашем случае
```
SELECT s.name, g.subject, g.grade
FROM students s
LEFT JOIN grades g
ON s.id = g.student_id
```
Задание:   
1. используя условие на выборку данных WHERE отберите только студентов 2 курса
2. используя оператор группирования данных `GROUP BY <имя поля>, <имя поля>` и функцию подсчета среднего `AVG`
определите среднюю оценку для группы полей - имя студента и дисциплина
```
Иван Информатика 4.5
Иван Математика  4
```
<hr>

### Задание 5. Выборка с условиями

Вставьте в таблицу `grades` еще несколько данных:  
```
Напишите и выполните запрос:
для полей (id, student_id, subject, grade)
вставьте значения
(6, 1, 'Математика', 2),
(7, 2, 'Математика', 3),
(8, 3, 'Информатика', 2),
(9, 3, 'Математика', 4),
(10, 3, 'Математика', NULL),
(11, 1, 'Информатика', 5);
```
Запустите `Xampp Control Panel` + Apache + mySql + Admin (mySql)   
Выберите таблицу `grades` для базы данных `grade-is221`  
Откройте вкладку `SQL` наберите запрос на выборку данных - каждый запрос открой те в свой вкладке браузера (5 вкладок)  
```
SELECT s.name, g.subject, g.grade FROM `grades` g
LEFT JOIN students s ON s.id = g.student_id
WHERE ..

Условия поиска (WHERE)
1) Сравнение (>, >=, =)
grade > 4 OR grade = 2

2) Принадлежность диапазону - попадание в определенный диапазон значений
grade BETWEEN 2 AND 3 
grade NOT BETWEEN 2 AND 3

3) Принадлежность множеству
grade IN (4,5)
grade NOT IN (4,5)

4) Соответствие шаблону
name LIKE '%р%' 
% - любая последовательность символов

5) Проверка на NULL
grade IS NULL
```
Отсортируйте выборку `SELECT * FROM grades` по полю `grade` в обратном порядке `DESC` - от большего значения к меньшему (ORDER BY) 

### Задание 6. Агрегатные функции для групп

Цель - познакомиться на практике с популярными агрегатными функциями:  
AVG - среднее значение, SUM - сумма значений, MIN - минимальное, MAX - максимальное значение, COUNT - количество значений  
использовать соединение двух таблиц по ключевым полям  
строить группы по нескольким группирующим признакам с ограничением.   

```
Соедините две таблицы grades и students по полям student_id (у grades) и id (у students)
Постройте выборку по полям: name (у students), subject и grade (у grades)
Добавьте поля вычисляющие агрегатные функции AVG(), MIN(), MAX(), COUNT() для поля grade (у grades)
Постройте группировку по полям name (у students), subject (у grades)
Добавьте ограничение на группу COUNT(*) > 1 (число записей в группе более 1)
Отсортируйте выборку по возрастанию средних оценок
```
<hr>

### Задание 7. Изменение таблицы и изменение данных в ней

Цель: научиться изменять уже существующую таблицу и изменять ранее добавленные данные в ней

1. В таблицу `students` добавьте колонку `last_name` тип - строка длиной 120 символов
```
оператор изменения таблицы - ALTER TABLE
шаблон
ALTER TABLE название_таблицы ADD COLUMN название_поля тип_поля;
```
2. Для таблицы `students` измените данные поля `last_name`:
для id = 1 - фамилия 'Иванов'
для id = 2 - фамилия 'Сидорова'
для id = 3 - фамилия 'Петров'   
```
оператор изменения данных - UPDATE
шаблон
UPDATE название_таблицы SET название_поля = значение поля WHERE условие;
```

